# Redirecionamento do GitHub Pages

Esta branch mantém o endereço `azdevcoder.github.io/leads-dashboard/` como porta de entrada e redireciona para a aplicação full-stack em `https://leadsdash-wrpfc2sq.manus.space`.

O artefacto estático anterior está preservado no histórico da branch `gh-pages`, no commit `c882525`.
